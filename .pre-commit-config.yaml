# Secret scanning only. See AGENTS.md "Never commit".
# One-time setup per clone:  task hooks:install
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
